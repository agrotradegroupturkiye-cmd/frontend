'use client'
import React from 'react';
import { useTranslation } from '../lib/i18n';
import AnimatedSection from './AnimatedSection';
import LanguageSwitcher from './LanguageSwitcher';

export default function Hero() {
  const { t } = useTranslation();
  return (
    <AnimatedSection delay={30}>
      <section className="w-full max-w-7xl mt-10 grid grid-cols-1 lg:grid-cols-12 gap-8 items-center">
        {/* Left: текст */}
        <div className="lg:col-span-7 px-4">
          <div className="inline-flex items-center gap-3 mb-6">
            <div className="w-12 h-12 rounded-lg bg-gradient-to-br from-blue-600 to-indigo-600 flex items-center justify-center text-white font-bold">CG</div>
            <div>
              <div className="text-sm text-gray-500">{t('siteName')}</div>
              <div className="text-xs text-gray-400">Агрегатор клининговых услуг</div>
            </div>
          </div>

          <h1 className="text-4xl md:text-5xl font-extrabold leading-tight mb-4 text-gray-900">
            {t('heroTitle')}
          </h1>

          <p className="text-lg text-gray-700 max-w-2xl mb-8">{t('heroSubtitle')}</p>

          <div className="flex flex-wrap gap-4 items-center">
            <a
              href="#services"
              className="inline-flex items-center px-6 py-3 bg-gradient-to-r from-blue-600 to-indigo-600 text-white rounded-xl shadow-lg hover:shadow-2xl transform hover:-translate-y-0.5 transition"
            >
              {t('orderNow')}
            </a>

            <a href="#cta" className="text-sm text-gray-700 hover:underline">Расчитать стоимость</a>

            <div className="ml-auto"><LanguageSwitcher /></div>
          </div>

          <div className="mt-8 grid grid-cols-2 gap-3">
            <div className="bg-white p-3 rounded-lg shadow flex items-center gap-3">
              <div className="w-10 h-10 flex items-center justify-center rounded bg-gray-50">⭐</div>
              <div>
                <div className="text-sm font-semibold">4.8</div>
                <div className="text-xs text-gray-500">Рейтинг сервиса</div>
              </div>
            </div>
            <div className="bg-white p-3 rounded-lg shadow flex items-center gap-3">
              <div className="w-10 h-10 flex items-center justify-center rounded bg-gray-50">⏱</div>
              <div>
                <div className="text-sm font-semibold">От 1 часа</div>
                <div className="text-xs text-gray-500">Быстрый выезд</div>
              </div>
            </div>
          </div>
        </div>

        {/* Right: иллюстрация / карточка промо */}
        <div className="lg:col-span-5 px-4">
          <div className="rounded-2xl overflow-hidden shadow-2xl transform hover:scale-101 transition">
            <div className="bg-gradient-to-br from-white to-gray-50 p-6">
              <div className="h-56 bg-[linear-gradient(90deg,#eef2ff,transparent)] rounded-xl flex items-center justify-center">
                {/* inline SVG — аккуратно выглядит без внешних ресурсов */}
                <svg width="220" height="220" viewBox="0 0 220 220" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden>
                  <rect x="10" y="10" width="200" height="200" rx="20" fill="#fff"/>
                  <g transform="translate(20,30)" fill="none" stroke="#c7d2fe" strokeWidth="3">
                    <rect x="0" y="0" width="160" height="100" rx="12" />
                    <path d="M8 120h144" stroke="#e6edf8" strokeWidth="6"/>
                  </g>
                </svg>
              </div>
              <div className="mt-4">
                <h4 className="text-lg font-semibold">Быстрая уборка в 1 клик</h4>
                <p className="text-sm text-gray-600 mt-2">Покажи адрес, выбери услугу — мы подберём исполнителя.</p>
              </div>
              <div className="mt-4 flex gap-3">
                <a href="#services" className="px-4 py-2 bg-white rounded-lg text-sm shadow">Смотреть услуги</a>
                <a href="#cta" className="px-4 py-2 bg-blue-600 text-white rounded-lg text-sm hover:bg-blue-700 transition">Заказать</a>
              </div>
            </div>
          </div>
        </div>
      </section>
    </AnimatedSection>
  );
}
